// services/api.ts
// 更新: リファクタリングにより新しい構造にリダイレクト
// このファイルは互換性のために残されています

// 新しい場所から全ての機能をエクスポート
export * from './api/common/environment';
export * from './api/common/request';
export * from './api/client-factory';

// 環境設定を新しい構造から取得
import { getApiConfig } from './api/common/environment';
import { ApiEnvironmentConfig } from '../types/api';

// API設定（互換性のため）
export const API_CONFIG: Record<string, ApiEnvironmentConfig> = {
  bitget: getApiConfig('bitget')
};

// 関数は新しい場所に移動しました

// 関数は新しい場所に移動しました

// 関数は新しい場所に移動しました

// 関数は新しい場所に移動しました

// 関数は新しい場所に移動しました
